// Cwd/transcript resolution domain barrel. Command-bearing submodule is `pub(crate) mod`
// so the crate root can name each command by its DEFINITION path.

pub(crate) mod commands;
pub(crate) mod scan;
